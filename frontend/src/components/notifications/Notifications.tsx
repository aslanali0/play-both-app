import api from "../api/api";
import { useAuth } from "../context/AuthContext";



const Notifications = () => {
  const { user } = useAuth()


  return (
    <div>

    </div>
  )

}

export default Notifications;
